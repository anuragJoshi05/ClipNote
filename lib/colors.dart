import 'package:flutter/material.dart';

const bgColor =
    Color(0xFF0B0B0B); // Another very subtle grey option, primarily black
const searchBarColor = Color(0xFF1F1F1F); // Dark grey, closer to black
const cardColor = Color(0xFF1F1F1F);
const black = Color(0xFF000000);
const white = Color(0xFFFFFFFF);
